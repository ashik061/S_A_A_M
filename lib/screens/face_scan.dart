import 'package:flutter/material.dart';

class FaceScan extends StatelessWidget {
  const FaceScan({super.key});

  static String routeName = 'FaceScan';

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
